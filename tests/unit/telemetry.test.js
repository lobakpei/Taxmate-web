const test=require('node:test');const assert=require('node:assert/strict');const T=require('../../src/core/telemetry');
test('Sentry payload scrub removes identity, inputs and request values',()=>{const x=T.scrubSentryEvent({user:{email:'x'},request:{url:'https://taxmate.uk/?amount=10',data:{amount:10},headers:{authorization:'secret'}},breadcrumbs:[{category:'ui.input',message:'60000'},{category:'navigation',message:'Tax'}],contexts:{state:{entries:[]}}});assert.equal(x.user,undefined);assert.equal(x.request.url,'https://taxmate.uk/');assert.equal(x.request.data,undefined);assert.deepEqual(x.breadcrumbs,[{category:'navigation',message:'Tax'}]);assert.equal(x.contexts,undefined);});
test('analytics permits only the small value-free taxonomy',()=>{assert.deepEqual(T.analyticsEvent('income_added'),{name:'income_added',params:{app_surface:'web'}});assert.throws(()=>T.analyticsEvent('income_amount_60000'));});
