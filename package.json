{
  "name": "taxmate-web",
  "version": "2.0.0-preview.2",
  "private": true,
  "description": "TaxMate modernisation and hardening candidate",
  "scripts": {
    "test": "node --test",
    "test:characterization": "node --test tests/characterization/*.test.js",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "test:rules": "node --test tests/rules/rules-source.test.js",
    "test:rules:emulator": "node scripts/run-rules-emulator.js",
    "test:all": "npm run test:characterization && npm run test:unit && npm run test:integration && npm run test:rules && npm run test:rules:emulator"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.1.0",
    "firebase-tools": "^15.27.0",
    "jszip": "3.10.1"
  }
}
