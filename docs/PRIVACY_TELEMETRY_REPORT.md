# Privacy, Analytics and Error Monitoring Report — Legal Gate 2026-08-19

Runtime inventory: local storage; Firebase Auth/Firestore/Storage/App Check; Google authentication; Stripe billing candidate; GA4; Sentry; Namecheap email forwarding; Microsoft Outlook; Google Fonts; jsPDF CDN dependencies. Apple authentication is intentionally removed. The private Outlook destination is not present in public source.

GA4 uses a fixed, small event taxonomy and no financial values, descriptions, IDs or receipt data. It is now off by default: the Google tag is loaded only after the user opts in from Settings, and consent can be withdrawn there. The telemetry module rejects unknown events and does not send a page view automatically. Sentry removes user identity, messages, request data, breadcrumbs, contexts and extras; exception messages are replaced with `Application error`, leaving only structural exception type and a minimised stack location.

The in-app and standalone privacy copy now describes Google sign-in only and documents Firebase regions, Stripe role, GA4 consent, Sentry minimisation, lawful bases, transfers, deletion scope, partnership retention and provider recovery/legal retention.

With explicit Founder approval, the existing GA4 property `541961931` / Web stream `15084238688` was configured for data minimisation. Enhanced Measurement is off; email redaction is on; URL query redaction covers 18 defensive keys; event and user retention are both two months; reset-on-new-activity is off; all four account data-sharing options are off; Google Signals and user-provided data collection remain disabled; granular location/device collection is off; and advertising personalisation is disallowed in all 307 regions. The application continues to use `client_storage=none`, no automatic page view, denied advertising consent/signals and only the allow-listed, value-free event taxonomy. Final browser validation found and fixed a declarative-action omission that had prevented the Settings consent checkbox from loading the Google tag. After the fix, the tag loaded only after opt-in and a fresh `upgrade_viewed` appeared in GA4 Realtime. Provider drill-down showed the allow-listed event, `app_surface`, GA automatic session/batch fields and `client_storage=none`; it showed no financial amount, business name, receipt, note, email or other bookkeeping content. GA4 received delivery is `PASS` for this candidate.

The existing EU Sentry project now has server-side data scrubbing and default scrubbers enabled, raw IP-address storage prevented, a defensive additional-sensitive-field list saved, Enhanced Privacy enabled and anonymous shared issues disabled. It is on the Developer plan, whose current provider pricing states a 30-day lookback; Replay remains unconfigured (zero usage and the event page still offers setup). A fresh scrubbed synthetic exception reached Sentry as `TAXMATE-9`. Its stored exception value was `Application error`; the UI showed no HTTP request, URL/query, referrer, User-Agent, email, business name, amount, receipt, note or breadcrumb section. Only structural exception/stack data, level and Sentry trace metadata remained. Sentry still derived coarse geography even though raw IP storage is disabled, so the received event is bookkeeping-content-minimal, not metadata-free. A separately inspected older production event predating this candidate still contains excessive request/user data; because RC.3 is not deployed, production telemetry must not be described as remediated. The candidate CSP continues to allow-list only the exact Sentry SDK and EU ingestion hosts.
