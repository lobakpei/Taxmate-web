# Known Limitations at Final Founder Preview

- The candidate is not deployed. No production data, rules, Functions, Stripe configuration or Hosting state changed.
- Existing TaxMate Firebase configuration is verified, but candidate Functions are not deployed and production was not used as staging. Previous Stripe sandbox evidence was invalidated after the refreshed Dashboard context still resolved to the wrong parent account; no account-specific Stripe IDs remain in the candidate. GA4 DebugView delivery and a scrubbed Sentry event were verified against the existing TaxMate accounts.
- Apple Sign-In is intentionally removed and out of scope; Google is the sole authentication provider in the candidate.
- Google browser sign-in, real cloud receipt/full-ZIP restore and deployed candidate App Check/Functions remain release-blocking. GA4 and Sentry delivery passed, but their provider privacy settings remain a release review item. Stripe TEST server lifecycle passes. Genuine two-client, offline, receipt, partnership and deletion behavior pass against the isolated Emulator Suite.
- Scottish income tax, VAT, companies, landlord bookkeeping and HMRC MTD submission are out of scope.
- Only published 2025-26 SA103S/SA104S mappings are bundled; future forms are not guessed.
- CSP is enforcing. Inline styles remain permitted to preserve the frozen UI. The exact Sentry SDK bundle and EU ingestion host are allow-listed; future endpoint changes must be explicit rather than widening policy.
- GitHub CLI is authenticated as `lobakpei` over HTTPS. Push/PR remains prohibited until explicit Founder approval.
- Root production dependency audit reports 0 vulnerabilities. Functions production dependencies report 8 moderate transitive advisories and 0 high/critical; npm's suggested complete fixes require incompatible major upgrades, so no forced rewrite was applied. Review again before deployment.
- Legal copy is engineering-reviewed, not legal advice or a statement of compliance. Founder/UK-adviser approval remains required for lawful bases, legitimate interests, consumer/business classification, cooling-off/refund wording and retention.
- ICO correspondence-address change is `ICO_UPDATE_CONFIRMED_PENDING_PUBLIC_PROPAGATION`. Production publication stays blocked until reference `ZC174150` shows the new non-residential/PO Box/alternative address. Exact `TaxMate` register casing is a minor administrative follow-up.
- The verified new correspondence address is not yet in the candidate because it is not public/provided; distance-selling identity/address information must be completed before paid production release.
- Support email is routed from public `support@taxmate.uk` through Namecheap forwarding to a private Microsoft Outlook mailbox. Forwarding logs/copies, Outlook retention/account type, access controls and provider terms/transfers remain unverified. GA4 currently has Enhanced Measurement enabled and URL query masking disabled; retention/sharing settings remain to review. The RC.3 Sentry synthetic payload is scrubbed, but provider-added trace/coarse-geography metadata and retention/IP processing settings remain to review. An older production event still contains request/URL-query/referrer/User-Agent/User fields; production remains unchanged until a separately approved deployment. The private destination address must not be published.
- Historical Git author metadata contains a personal Gmail address. Decide the future no-reply/business author identity and any history treatment before pushing; no history was rewritten here.
- Stripe source/emulator policy tests cover server checkout, Terms consent, promotions, refusal, cancellation, refunds and webhook entitlement. Correct-account hosted Stripe TEST evidence is blocked until the Dashboard session resolves to the independent TaxMate account; no earlier account evidence is accepted. Billing Portal browser UX and durable email confirmation also remain outstanding.
- Founder launch policy is Stripe Tax off, no separately charged VAT and no VAT invoice/amount. VAT-registration threshold and any future VAT treatment remain operational monitoring items, not an automated registration action.
- The isolated `taxmate-staging` project has Firestore/rules, Google-only Auth, App Check registration and Hosting. Firebase requires explicit Blaze billing before staging Functions and Storage can be enabled, so real cloud receipts/full-ZIP restore and deployed callable App Check remain blocked.
