'use strict';
const path=require('node:path'),{spawnSync}=require('node:child_process'),{localToolEnvironment,localBinary}=require('./local-tool-runtime'),{prepare}=require('./emulator-test-config'); const root=path.resolve(__dirname,'..');
const {env}=localToolEnvironment(root);
const firebase=localBinary(root,path.join('node_modules','.bin','firebase.cmd')),isolated=prepare(root,{...process.env,TAXMATE_FIRESTORE_EMULATOR_PORT:process.env.TAXMATE_FIRESTORE_EMULATOR_PORT||'28080',TAXMATE_STORAGE_EMULATOR_PORT:process.env.TAXMATE_STORAGE_EMULATOR_PORT||'29199'}); const command=`"${firebase}" emulators:exec${isolated.arg} --project demo-taxmate --only firestore,storage "node --test tests/rules/emulator.test.js"`; const child=spawnSync(command,{cwd:root,env,stdio:'inherit',shell:true});isolated.cleanup();process.exit(child.status==null?1:child.status);
